(clear-all)

(defun run-it ()
  (model-output "Current float size is: ~S" *time-size-current-type*)
  (model-output "Available float sizes are: ~s" *time-size-test-list*))

(define-model test 
    (sgp-fct (list :v *out-file*)))
